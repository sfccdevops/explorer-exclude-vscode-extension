name: Marketplace Publishing Disabled

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm manual publishing policy
        run: echo "Marketplace publishing is intentionally manual."
